# Nightly QA report — 2026-06-13

Starting commit · `97cf10f` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~25 min

## Summary

**5 findings · 0 auto-fixed (§5) · 2 reported · 3 informational**
Codex sweep: 0 fix / 1 reply / 1 skip / 0 deferred

## Build health

| Check | Result |
| --- | --- |
| TypeScript (all packages) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint both apps | ✅ CLEAN (next-lint deprecation warning only — not an error) |
| Purchase funnel routes (cart→checkout→/track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ All hrefs resolve to `page.tsx` |
| Server actions — orphan check | ✅ All `'use server'` files have callers |
| Form button explicit `type=` | ✅ All form buttons have explicit type |
| `<img>` CLS (missing width/height) | ✅ None found |
| Icon-only buttons without `aria-label` | ✅ None found |
| Tap targets h-6/h-7 on touch elements | ✅ None found |
| Defensive SELECT (payment_method / COD / modifier_groups) | ✅ All wrapped with null checks and fallback logic |
| i18n drift RO vs EN | ⚠️ RO=937 / EN=927 / Δ=10 (at threshold — monitor) |
| Migration sequence integrity | ⚠️ 20 duplicate prefix groups (carry-over — see P2 below) |

---

## §5 Auto-fixed (PRs)

None — no new auto-fixable issues found this run.

---

## §6 Codex sweep

Window `2026-06-11T00:13:00Z` → `2026-06-13T00:25:00Z` (48 h cap) · PRs scanned: 5 (#921, #885, #786, #782, #255)

| PR | file:line | classification | action | link |
| --- | --- | --- | --- | --- |
| #921 | `apps/restaurant-web/src/app/oferta-flota/page.tsx:72` | REPLY — bug valid on old branch, already fixed in main via merged PR #922 | Replied to comment 3397476571 | [#discussion_r3397476571](https://github.com/IulianMoldoveanu/RSHIR-Platform/pull/921#discussion_r3397476571) |
| #786 | `demo-pack/video-production/PRODUCTION-GUIDE-HEYGEN.md:169` | SKIP — already processed (ID 3316208402, reply posted 2026-05-30) | — | — |

**Notes on PR #921:** The Codex comment was valid — the PR's `page.tsx` (branch `fix/fleet-pdf-debug-error`) had an `onClick` directly on a `<button>` inside a Server Component with no `'use client'` directive. However, this was fixed in the superseding PR #922 (merged 2026-06-11T16:38:58Z), which created `print-button.tsx` as a proper `'use client'` island. PR #921 is now stale and can be closed.

---

## Report-only findings (prioritized)

### [P2] 20 migration duplicate prefix groups (carry-over)

**Where:** `supabase/migrations/*.sql`
**Symptom:** 20 groups of 2–3 files share the same `YYYYMMDD_NNN` prefix. Non-deterministic apply order on a fresh Supabase restore.
**Groups (new since last report):** `20260601_001×2`, `20260630_030×2` (added by recent fleet/courier migrations).
**Suggested fix:** rename the later files to the next available sequence (`_002`, `_003`, etc.).
**Why not auto-fixed:** migration renames require re-applying on all Supabase environments; human must verify before rename.

### [P2] Admin modifier-groups CRUD UI missing (carry-over, 4th run)

**Where:** `apps/restaurant-admin/src/app/dashboard/menu/modifier-groups/` — does not exist
**Symptom:** Migration `20260505_001_modifier_groups.sql` shipped and `restaurant_menu_modifier_groups` is queried in `menu/actions.ts` and `menu/page.tsx`, but there is no operator CRUD page.
**Suggested fix:** ~1-day effort: add `modifier-groups/page.tsx` following the existing `promos/` pattern (list + add/edit dialog).
**Why not auto-fixed:** exceeds 3-file/50-line cap; requires new page with actions, form, and revalidation.

---

## §7 Improvement suggestions

**Non-bug. Tied to UX audit / conversion research / roadmap.**

1. **[HIGH] Checkout deferred P0s are now shippable** — `docs/UI_UX_AUDIT_PROGRESS.md` §4 lists two P0 items still deferred: *sticky-bottom CTA on mobile* and *cart-summary collapsible accordion*. Pilot is live with real orders. Both items directly reduce checkout drop-off and are well-understood technically (≤2 days). Recommend scheduling for next sprint.

2. **[MEDIUM] API route test coverage gap** — 20+ `route.ts` files in `apps/restaurant-web/src/app/api/` have no regression specs. Highest-risk untested routes:
   - `api/checkout/netopia/intent/route.ts` — Netopia payment init, no test
   - `api/checkout/otp/request/route.ts`, `api/checkout/otp/verify/route.ts` — OTP gate
   - `api/checkout/pre-order/route.ts` — pre-order flow
   - `api/checkout/promo/validate/route.ts` — promo code validation
   - `api/public/v1/orders/[id]/route.ts` — public order status (used by track page fallback)
   These are purchase-funnel critical and unprotected by automated regression.

3. **[MEDIUM] i18n drift at threshold** — RO has 10 more keys than EN (937 vs 927). At exactly the monitoring threshold. No auto-fix triggered (limit is >10), but worth a manual sync pass before the gap widens. Key areas to check: any new storefront copy added after 2026-05-30.

4. **[LOW] PR #921 should be closed** — The `fix/fleet-pdf-debug-error` branch is superseded by the merged `fix/fleet-offer-html-pivot` (PR #922). Leaving it open adds noise to the PR list.

5. **[LOW] `docs/UI_UX_AUDIT_PROGRESS.md` §8 deferred** — "P1 sound + tab title flash on new orders queue" has been deferred for 4+ runs. Browser Notification API + `document.title` cycling is a self-contained ~30-line addition. Low risk.

6. **[INFO] Competitive research at `docs/research/2026-04-28-competitive-and-ux.md`** — recommendations for social proof (ratings/reviews surface) and loyalty (punch-card mechanic) are not yet implemented. Marketplace MVP (PR #782, do-not-merge) will satisfy the aggregator recommendation once pilots are live. The loyalty/review surface remains an open opportunity for post-pilot conversion improvement.

---

## Run metadata

| Step | Result |
| --- | --- |
| `pnpm install --frozen-lockfile` | ✅ exit 0 (12.5s) |
| `pnpm -r typecheck` | ✅ exit 0 — all 15 packages clean |
| `pnpm --filter @hir/restaurant-web build` | ✅ exit 0 |
| `pnpm --filter @hir/restaurant-admin build` | ✅ exit 0 |
| `pnpm -r lint` | ✅ exit 0 (next-lint deprecation warning, not an error) |
| Admin sidebar href walk (58 hrefs) | ✅ all resolve to page.tsx |
| i18n key count (python3) | RO=937 / EN=927 / Δ=10 |
| Form buttons type= check | ✅ all explicit |
| `<img>` CLS grep | ✅ clean |
| Codex sweep (5 open PRs) | 1 reply, 1 skip |
| §6 reply POST | ✅ comment 3406912177 posted |
