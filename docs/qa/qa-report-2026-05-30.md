# Nightly QA report — 2026-05-30

Starting commit · `e0fd559` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~35 min

## Summary

**8 findings · 6 auto-fixed (§5, 2 PRs) · 4 reported · 3 informational**
Codex sweep: 0 fix / 1 reply / 1 skip / 0 deferred

## Build health

| Check | Result |
| --- | --- |
| TypeScript (all packages) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint both apps | ✅ CLEAN |
| Purchase funnel routes (cart→checkout→/track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ All hrefs resolve to `page.tsx` |
| Defensive SELECT (payment_method / COD / modifier_groups) | ✅ All wrapped in try/catch |
| i18n drift RO vs EN | ✅ RO=951 / EN=949 / Δ=2 (threshold 10) |
| Migration sequence integrity | ⚠️ 19 duplicate prefix groups (carry-over — see P2 below) |

---

## §5 Auto-fixed (PRs)

- **#806** `fix(a11y): add width/height to storefront gallery + city listing img tags (CLS)` — `presentation-gallery.tsx` (thumbnail 400×400, lightbox 1200×900), `orase/[citySlug]/page.tsx` (tenant logo 56×56)
- **#807** `fix(a11y): add width/height to admin image preview img tags (CLS)` — `presentation-client.tsx` (gallery thumb 400×400), `branding-form.tsx` (logo preview 200×48), `StepBrand.tsx` (onboard logo 112×112)

---

## §6 Codex sweep

Window 2026-05-28T00:30:00Z → 2026-05-30T00:30:00Z (48 h cap) · PRs scanned: 3 (#786, #742, #724)

| PR | file:line | classification | action | link |
| --- | --- | --- | --- | --- |
| #786 | `demo-pack/video-production/PRODUCTION-GUIDE-HEYGEN.md:169` | REPLY — path outside restaurant-web/admin scope | Replied to comment 3316208402 | [#discussion_r3316208402](https://github.com/IulianMoldoveanu/RSHIR-Platform/pull/786#discussion_r3316208402) |
| #742 | courier PR (apps/restaurant-courier) | SKIP — off-limits | — | — |
| #724 | nightly-qa PR | SKIP — 0 Codex comments | — | — |

---

## Report-only findings (prioritized)

### [P2] 19 migration duplicate prefix groups (carry-over)

**Where:** `supabase/migrations/*.sql`
**Symptom:** 19 groups of 2–3 files share the same `YYYYMMDD_NNN` prefix. Non-deterministic apply order on a fresh Supabase restore.
**Groups:**
`20260504_{001×3, 002×2, 003×2}`, `20260505_001×3`, `20260506_{001×2, 013×2}`, `20260507_{009×2, 011×2, 012×2}`, `20260508_{001×2, 002×3, 003×2}`, `20260517_001×3`, `20260522_004×2`, `20260605_{003×3, 004×3}`, `20260606_003×2`, `20260608_003×3`, `20260609_001×2`
**Suggested fix:** rename the later files to the next available sequence (`_002`, `_003`, etc.).
**Why not auto-fixed:** migration renames require re-applying the affected statements on all Supabase environments; human must verify before rename.

### [P2] Admin modifier-groups CRUD UI missing

**Where:** `apps/restaurant-admin/src/app/dashboard/menu/modifier-groups/` — does not exist
**Symptom:** Migration `20260505_001_modifier_groups.sql` has shipped and `restaurant_menu_modifier_groups` is queried in `menu/actions.ts` and `menu/page.tsx`, but there is no dedicated CRUD page for operators to manage modifier groups (add/edit/delete).
**Suggested fix:** ~1-day effort: add a `modifier-groups/page.tsx` following the existing `promos/` pattern (list + add/edit dialog).
**Why not auto-fixed:** exceeds 3-file/50-line cap; requires new page with actions, form, and revalidation.

### [P2] Remaining CLS img tags — report only

The following `<img>` elements use dynamic/unknown intrinsic dimensions (inline `style=` sizing or unknown source aspect ratio). Not auto-fixable without introducing incorrect layout hints:
- `apps/restaurant-web/src/app/r/[code]/page.tsx:276` — partner hero image (`style={{ width: '100%', maxWidth: 920 }}`) — consider using Next.js `<Image fill>` or adding explicit dimensions from the partner settings schema.
- `apps/restaurant-admin/src/components/feedback-modal.tsx:280` — user screenshot preview (`max-h-40 w-full`) — dynamic camera capture, no fixed dimensions available.
- `apps/restaurant-web/src/app/(storefront)/poveste/page.tsx:163` — restaurant cover with no `width=`/`height=` attribute (uses CSS sizing only).

### [INFO] Codex comment on `demo-pack/video-production/PRODUCTION-GUIDE-HEYGEN.md`

Codex flagged that Vimeo Plus is a legacy plan (no longer offered to new customers). The step on line 169 of `PRODUCTION-GUIDE-HEYGEN.md` references signing up for Vimeo Plus at $7/mo. Iulian should update this to use Vimeo Basic (free) or a currently purchasable paid plan before distributing the guide. Not in automated scope.

---

## §7 Improvement suggestions

**Non-bug. Tied to UX audit / conversion research / roadmap.**

1. **Deferred P0 from UX audit — checkout sticky CTA + cart accordion** (`docs/UI_UX_AUDIT_PROGRESS.md` §4): "P0 sticky-bottom CTA on mobile" and "P0 cart-summary collapsible accordion" are explicitly P0 in the audit and marked deferred. The checkout flow is now stable enough post-OTP/PSP to tackle these. Estimated effort: 1–2 days for both.

2. **Modifier groups admin CRUD** — migration `20260505_001` shipped weeks ago; no operator can manage modifier groups from the admin UI. This is the highest-value gap between schema and UI. Estimated effort: ~1 day.

3. **Integration providers — no exercised adapters in dev**: `integration_providers` table is read in 5+ places but no seed/dev data configures a live POS adapter. The integration-core package scaffolding exists but hasn't been exercised end-to-end. Suggest adding a dev seed row (STANDALONE mode) to the next Supabase migration.

4. **Research docs**: `docs/research/2026-04-29-gloriafood-final-synthesis-and-adaptation-plan.md` lists GloriaFood features as "to adapt." Most high-value items (free delivery bar, social proof, upsell rail, reorder) are now shipped. Remaining open items: **aggregator SEO pages** (`/restaurante`) from PR #782 (DRAFT, do-not-merge until 5-10 tenants live) and **scheduled pre-orders** (migration shipped in `20260609_001_pre_orders.sql`).

5. **Untested public API routes**: 44 route handlers in `apps/restaurant-web/src/app/api/**`. Only `checkout/pricing` has integration tests visible via grep. High-impact routes with no visible regression spec: `checkout/confirm/route.ts`, `checkout/otp/verify/route.ts`, `track/[token]/route.ts`, `public/v1/orders/route.ts`.

---

## Run metadata

| Step | Exit code | Notes |
| --- | --- | --- |
| `pnpm install --frozen-lockfile` | 0 | 11.7s |
| `pnpm -r typecheck` | 0 | All 15 scoped packages |
| `pnpm --filter @hir/restaurant-web build` | 0 | After storefront CLS fix |
| `pnpm --filter @hir/restaurant-admin build` | 0 | After admin CLS fix |
| `pnpm --filter @hir/restaurant-web lint` | 0 | No warnings |
| `pnpm --filter @hir/restaurant-admin lint` | 0 | No warnings |
| Codex sweep — PR #786 review comments | — | 1 comment found, 1 replied |
| Codex sweep — PR #742 review comments | — | Courier (off-limits), skipped |
| Codex sweep — PR #724 review comments | — | 0 Codex comments |

Prior QA PRs still open (not merged): #724 (2026-05-23 report). Carry-over findings from that report (migration duplicates, orphaned server actions) are unchanged.
