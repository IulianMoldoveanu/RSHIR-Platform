# Nightly QA report — 2026-06-20

Starting commit · `8ff5faf` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~25 min

## Summary

**7 findings · 0 auto-fixed (§5) · 4 reported · 3 informational**
Codex sweep: 0 fix / 0 reply / 0 skip / 0 deferred

## Build health

| Check | Result |
| --- | --- |
| TypeScript (all 16 workspace projects) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint restaurant-web | ✅ CLEAN |
| ESLint restaurant-admin | ✅ CLEAN |
| Purchase funnel routes (cart→checkout→/track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ All hrefs resolve (query-param and API-route variants are false positives) |
| Defensive SELECT (payment_method / COD columns) | ✅ All usages in try/catch-wrapped files |
| i18n drift RO vs EN | ✅ RO=968 / EN=968 / Δ=0 |
| Buttons without `type=` inside `<form>` | ✅ 0 found (Python AST-level scan) |
| `<img>` without both `width=` and `height=` | ✅ 0 found |
| Icon-only `<button>` without `aria-label` | ✅ 0 found |
| Tap targets h-6/h-7 on touch elements | ✅ 0 found |
| Migration sequence integrity | ⚠️ 2 NEW duplicate prefix groups (P2 — see below) |

---

## §5 Auto-fixed (PRs)

None — all static checks pass.

---

## §6 Codex sweep

Window `2026-06-18T00:00:00Z` → `2026-06-20T00:00:00Z` (48 h cap; `last_run_at` was `2026-05-30T00:35:00Z` → capped to 48 h)  
PRs scanned: 7 (#940, #939, #923, #921, #885, #782, #255)

| PR | file:line | classification | action | note |
| --- | --- | --- | --- | --- |
| All scanned | — | No Codex comments within 48 h window | SKIP (all) | — |

**Informational — stale Codex comments outside window:**  
PR #940 has 2 Codex review comments (created 2026-06-17T08:10:19Z — outside the 48 h sweep cap) on `supabase/migrations/20260617_001_security_advisor_safe_hardening.sql`. They flag that the migration runs `ALTER FUNCTION` on `copilot_tenant_config_set_updated_at` (line 54) and on functions first created in the `20260630_*` series (line 76). On a clean migration replay, `20260617` runs *before* those functions exist, so the migration would abort. The PR is open and unmerged — recommend reviewing/fixing before merge. (Outside 48 h sweep window; not actioned per §6.1 rule.)

---

## Report-only findings (prioritized)

### [P2] NEW: Two additional migration duplicate prefix groups

**Where:** `supabase/migrations/`
**Symptom:** Two new duplicate prefix groups appeared since the last QA report (2026-05-30 logged 19 groups):
- `20260630_030`: `20260630_030_courier_dropoff_notes.sql` and `20260630_030_courier_offer_push_trigger.sql`
- `20260616_001`: `20260616_001_agent_runs_embedding.sql` and `20260616_001_reseller_min_vendors_threshold.sql`

Total duplicate groups now: ~21 (previously 19). On a clean Supabase restore the apply order within each prefix group is non-deterministic.
**Suggested fix:** Rename the later file in each pair to the next available suffix (`_031` / `_002`).
**Why not auto-fixed:** Migration renames require verifying that the rename hasn't already been applied to any Supabase environment before renaming; human sign-off required.

### [P2] PR #940 — migration ordering may abort on clean replay (carry-forward from stale Codex review)

**Where:** `supabase/migrations/20260617_001_security_advisor_safe_hardening.sql:54,76`
**Symptom:** `ALTER FUNCTION copilot_tenant_config_set_updated_at` targets a function with no CREATE in the repo. Also, lines ~76–120 ALTER functions first defined in the `20260630_*` series. When migrations are applied in filename order, `20260617` precedes `20260630` — ALTER on a non-existent function throws `ERROR: function does not exist`.
**Impact:** Does not affect current prod (migration was applied via Management API directly). Breaks any new environment provisioning or Supabase fork.
**Suggested fix:** Wrap each `ALTER FUNCTION` with an existence guard:
```sql
DO $$ BEGIN
  PERFORM set_config('search_path', 'pg_catalog, public, extensions', false)
  FROM pg_proc WHERE proname = 'copilot_tenant_config_set_updated_at';
END $$;
```
Or move the hardening migration date to `20260631` (after all `20260630` migrations).
**Why not auto-fixed:** Protected path — `supabase/migrations/**` is read-only for this agent.

### [P2] Admin modifier-groups CRUD UI missing (carry-over from 6+ runs)

**Where:** `apps/restaurant-admin/src/app/dashboard/menu/` — no `modifier-groups/` subfolder
**Symptom:** Migration `20260505_001_modifier_groups.sql` shipped months ago and `restaurant_menu_modifier_groups` is queried in `menu/actions.ts` and `menu/page.tsx`, but operators cannot manage modifier groups (create/edit/delete) from the admin UI. The existing `modifiers-panel.tsx` is for item-level modifiers only.
**Suggested fix:** New page following the `promos/` CRUD pattern — list + add/edit dialog, reuses `item-form-dialog.tsx` patterns. ~1-day effort.
**Why not auto-fixed:** Exceeds 3-file/50-line cap; new page + actions + form.

### [P1-INFO] PR #940 open — security hardening not yet merged

PR #940 (`security(qfme): safe Supabase advisor hardening`) applies already-live prod SQL to the repo for git↔prod parity. It has been open since 2026-06-17. The Codex ordering concern above is blocking safe merge. **No action by this agent** (migration scope) — flagging for human attention.

---

## §7 Improvement suggestions

Non-bug. Tied to UX audit / conversion research / roadmap.

### 1. Deferred P0s from UX audit (still not shipped)

`docs/UI_UX_AUDIT_PROGRESS.md` §4 Checkout:
- **P0 sticky-bottom CTA on mobile** — deferred "needs careful mobile-Safari testing." Post-critical-path stabilisation, this is now the highest-conversion-impact remaining item.
- **P0 cart-summary collapsible accordion** — deferred "restructures checkout step 1." Both items together estimated 1–2 days effort.

### 2. Migration duplicate cleanup

21 duplicate prefix groups now logged across 6 QA runs. The backlog is growing (+2 this run). A one-time rename pass (with `supabase db diff` verification) would eliminate future confusion.

### 3. PFA dashboard routing blockers (from PR #939 deferred)

Two activation blockers noted in PR #939 body:
- `/pfa-dashboard` links target a different feature flag → 404 in current state.
- `requireFleetManager()` resolves via `owner_user_id` but PFA context needs `pfa_owner_user_id` — wrong fleet returned for PFA operators.

`apps/restaurant-admin/src/app/dashboard/admin/pfa-pool/` exists for admin oversight but there's no self-serve PFA dashboard surface. Schedule for the PFA onboarding sprint.

### 4. Integration providers — no exercised adapters

`packages/integration-core/src/adapters/smartcash.ts` references the SmartCash POS (Cluj). No seed/dev row in `integration_providers` exercises this end-to-end. If the adapter scaffolding hasn't been tested against a real POS, add a dev seed row in STANDALONE mode to catch any breakage before deployment.

### 5. Untested public API routes

47 `route.ts` handlers in `apps/restaurant-web/src/app/api/`. 10 have test files. High-risk routes with no visible spec:
- `api/track/[token]/route.ts` — critical customer-facing status
- `api/fleet-offer-pdf/route.ts` — PDF generation (already had a prod failure, see PRs #918/#920/#922)
- `api/webhooks/netopia/route.ts` and `api/webhooks/viva/route.ts` — payment confirmation paths (highest money risk)

Note: The webhook routes have test files added in PR #936 — ✅. Remaining gap is `track/[token]` and `fleet-offer-pdf`.

### 6. `next lint` deprecation notice

Both apps emit: ``next lint` is deprecated and will be removed in Next.js 16. Use `npx @next/codemod@canary next-lint-to-eslint-cli .` to migrate.` No linting impact now; worth scheduling before the Next.js 16 upgrade.

---

## Run metadata

| Step | Exit code | Notes |
| --- | --- | --- |
| `pnpm install --frozen-lockfile` | 0 | 11.6s |
| `pnpm -r typecheck` | 0 | 16 workspace projects |
| `pnpm --filter @hir/restaurant-web build` | 0 | Clean |
| `pnpm --filter @hir/restaurant-admin build` | 0 | Clean |
| `pnpm --filter @hir/restaurant-web lint` | 0 | No errors |
| `pnpm --filter @hir/restaurant-admin lint` | 0 | No errors |
| i18n key drift check | — | RO=968 EN=968 Δ=0 |
| Defensive SELECT scan | — | All try/catch |
| Python button-in-form scan | — | 0 untyped buttons in forms |
| Migration sequence check | — | 2 new duplicate groups |
| §6 Codex sweep (48 h window) | — | 0 new comments in scope |
| Total wall-clock | — | ~25 min |
