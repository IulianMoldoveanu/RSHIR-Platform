# Nightly QA report — 2026-06-06

Starting commit · `1f4241e` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~30 min

## Summary

**5 findings · 1 auto-fixed (§5, PR #884) · 3 reported · 1 informational**
Codex sweep: 0 fix / 0 reply / 0 skip / 0 deferred

---

## Build health

| Check | Result |
|---|---|
| TypeScript (all packages) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint both apps | ✅ CLEAN |
| Purchase funnel routes (cart → checkout → /track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ `/login`, `/signup`, `/dashboard/admin/observability/function-runs` all resolve to `page.tsx` |
| Defensive SELECT (payment_method / COD) | ✅ `payment-mode.ts` has legacy `cod_enabled` fallback path |
| i18n drift RO vs EN | ✅ delta ~7 keys (threshold 10) |
| Migration sequence integrity | ⚠️ 20 duplicate prefix groups (carry-over from prior reports) |
| Form `<button>` implicit-submit check | ✅ All buttons inside `<form>` have explicit `type=` |

---

## §5 Auto-fixed (PRs)

### PR #884 — `fix(a11y): CLS img dimensions + search-clear tap target`

Branch: `fix/a11y-cls-tap-targets-2026-06-06`

| File | Fix |
|---|---|
| `apps/restaurant-web/src/app/(storefront)/poveste/page.tsx:163` | Added `width={1200} height={500}` to eager LCP cover `<img>` — was causing CLS on the brand-story page |
| `apps/restaurant-admin/src/app/dashboard/onboarding/wizard/client.tsx:688` | Added `width={64} height={64}` to wizard logo preview `<img>` — CSS `h-16 w-16` was already correct but HTML dimensions were absent |
| `apps/restaurant-web/src/components/storefront/menu-list.tsx:75` | Bumped search-clear `<motion.button>` from `h-7 w-7` (28 px) to `h-9 w-9` (36 px) — below WCAG 2.5.5 / HIR 36 px touch floor |

Safety net: `pnpm -r typecheck` ✅ · `pnpm --filter @hir/restaurant-{web,admin} build` ✅ · 3 files, 5 insertions, 1 deletion.

---

## §6 Codex sweep

Window `2026-06-04T00:00:00Z` → `2026-06-06T00:00:00Z` (48 h cap, anchored to prior `last_run_at` 2026-05-30 but floored to 48 h)  
PRs scanned: 4 open (#255, #742, #782, #786)

| PR | file:line | classification | action | notes |
|---|---|---|---|---|
| #742 | `packages/integration-core/src/payment/netopia.ts:163` | outside 48 h window (2026-05-25) | SKIP | comment id 3299122344 |
| #742 | `packages/integration-core/src/payment/netopia.ts:291` | outside 48 h window (2026-05-25) | SKIP | comment id 3299122350 |
| #742 | `apps/restaurant-web/src/app/api/webhooks/viva/route.ts:64` | outside 48 h window (2026-05-25) | SKIP | comment id 3299122352 |
| #786 | `demo-pack/video-production/PRODUCTION-GUIDE-HEYGEN.md:169` | already processed (id 3316208402) | SKIP | handled 2026-05-30 |

**0 new Codex actions this run.**

### Outstanding Codex notes (for information, not actioned this run)

The three PR #742 Codex comments remain unprocessed (outside 48 h window but not yet fixed). All three touch `packages/integration-core` (Netopia payment adapter) which is outside the restaurant-web/admin scope for auto-fix, but worth human attention before #742 merges:

- **Netopia endpoint typo** — `POST /payment/card` should be `POST /payment/card/start` (Codex P1, id 3299122344, line 163)
- **Empty notifyUrl** — when no `notifyUrl` is passed the code forces `""`, breaking Netopia payment callbacks (Codex P1, id 3299122350, line 291)
- **Viva webhook 400 on unknown events** — Viva retries on any non-2xx; should return 200 for dropped events (Codex P2, id 3299122352, line 64)

These comments are on PR #742 (`fix/courier-avatar-error-mapper`) which touches `packages/integration-core/src/payment/` — within restaurant-web review scope indirectly. Recommend Iulian review before merging #742.

---

## Report-only findings (prioritized)

### [P2] Dynamic-dimension `<img>` tags — 3 remaining

Previous run noted these; no change since 2026-05-30:

- **`apps/restaurant-web/src/app/r/[code]/page.tsx:276`** — partner hero image (`style={{ width: '100%', maxWidth: 920 }}`). No fixed intrinsic size known at build time. Suggested fix: move to Next.js `<Image fill>` with a wrapper `div` that has a fixed aspect ratio, or query the partner `hero_url` metadata for dimensions.
- **`apps/restaurant-admin/src/components/feedback-modal.tsx:280`** — camera screenshot preview (`max-h-40 w-full`). Dynamic capture with unknown dimensions. Acceptable pattern; not fixable without knowing content dimensions.
- **`apps/restaurant-admin/src/app/dashboard/feedback/[id]/page.tsx:171`** — screenshot viewer (`max-h-[600px] w-full`). Same as above.

### [P2] 20 migration duplicate prefix groups (carry-over)

Same as prior run — 20 groups of 2–3 files sharing the same `YYYYMMDD_NNN` prefix. Non-deterministic apply order on a fresh Supabase restore.

Groups affected (same as 2026-05-30 report):
`20260504_{001×3, 002×2, 003×2}`, `20260505_001×3`, `20260506_{001×2, 013×2}`, `20260507_{009×2, 011×2, 012×2}`, `20260508_{001×2, 002×3, 003×2}`, `20260517_001×3`, `20260522_004×2`, `20260605_{003×3, 004×3}`, `20260606_003×2`, `20260608_003×3`, `20260609_001×2`

Suggested fix: rename later files to next available sequence. Requires human verification before applying to all Supabase environments.

### [P2] Admin modifier-groups CRUD UI missing (carry-over)

Migration `20260505_001_modifier_groups.sql` shipped; `restaurant_menu_modifier_groups` is referenced in menu actions + page. No `/dashboard/menu/modifier-groups/` page exists. ~1-day effort to build (follow the `promos/` pattern). Deferred by prior runs; still unaddressed.

### [INFO] Netopia adapter comments on PR #742

Three Codex P1/P2 comments (from 2026-05-25) on `packages/integration-core/src/payment/netopia.ts` are outside the nightly auto-fix window. Summary in §6 above. The PR is open and not draft — recommend reviewing before merge.

---

## §7 Improvement suggestions

Non-bug. Tied to UX audit / conversion research / roadmap.

1. **Deferred UX audit P0s (§4 Checkout)** — `docs/UI_UX_AUDIT_PROGRESS.md` lists "P0 sticky-bottom CTA on mobile" and "P0 cart-summary collapsible accordion" as explicitly deferred. Checkout flow is stable post-PSP. Both are likely ≤1 day each. These have been deferred for 3+ consecutive reports; worth scheduling.

2. **Modifier groups CRUD (§9 Menu)** — The `modifier-groups` migration shipped 5 weeks ago but no admin UI exists. The `promos/` pattern is already in-repo and can be cloned. Estimated ~150 lines for list + add/edit dialog. Low regression risk, high operator value.

3. **PR #742 `fix/courier-avatar-error-mapper`** — This PR has been open since 2026-05-25 with 3 unresolved Codex review comments on the Netopia adapter. The avatar error fix itself is solid; the Netopia issues are in the same package and likely worth addressing before merge. See §6 for specifics.

4. **Marketplace MVP (PR #782)** — Draft PR open since 2026-05-27. The marketplace pages hardcode RO/EN strings in `marketing-shell.tsx` (acknowledged in the PR body — deferred as followup #9). The PR also adds `supabase/functions/marketplace-directory-refresh/` which needs a cron wiring migration (`20260629_002_...`). When the pilot greenlight fires, the string promotion to `t()` should be the first PR.

5. **i18n key drift** — Current RO ≈906 keys, EN ≈899, delta ~7 (below the 10-key alert threshold). The dictionary is close enough to flag: any new feature that adds RO copy should add EN in the same commit to keep drift under control.

6. **`apps/restaurant-web/src/app/api/**/route.ts` coverage** — The following API routes exist but have no adjacent `*.test.ts` regression spec: `/api/checkout/promo.ts`, `/api/track/[token]/route.ts`, `/api/newsletter/confirm/route.ts`. The `/api/checkout/intent/route.ts` already has 3 test files — that pattern should spread to the others.

---

## Run metadata

| Step | Command | Exit code |
|---|---|---|
| Preconditions | `git remote -v`, `git status`, `pnpm --version` | 0 |
| Concurrency check | `git log --all --since='30 minutes ago'` | 0, no recent activity |
| §4A TypeScript | `pnpm -r typecheck` | 0 (clean) |
| §4B Build web | `pnpm --filter @hir/restaurant-web build` | 0 (clean) |
| §4B Build admin | `pnpm --filter @hir/restaurant-admin build` | 0 (clean) |
| §4C Lint | `pnpm -r lint` | 0 (clean, 1 Node warning on pkg type, pre-existing) |
| §4D Link walk | grep hrefs in sidebar/layout | All resolve |
| §4E i18n | key count delta | ~7 (threshold: 10) |
| §4F Server actions | grep `'use server'` | 20+ files, all have callers |
| §4G Submit buttons | grep `<button` without `type=` in forms | 0 issues |
| §4H Defensive SELECT | grep `payment_method`, `cod_enabled` | Wrapped ✅ |
| §4I Accessibility | img/button sweep | 3 fixed (#884), 3 dynamic (REPORT) |
| §4J Migrations | duplicate prefix check | 20 groups (carry-over) |
| §5 Safety net | `pnpm -r typecheck` + builds after fix | 0 (clean) |
| §6 Codex sweep | 4 open PRs | 0 comments in 48 h window |
