# Nightly QA report — 2026-07-18

Starting commit · `5aeb979` (fix(marketplace): repair ai_jobs settlement enqueue #949) · Apps scanned: `apps/restaurant-web`, `apps/restaurant-admin` · Time elapsed: ~25 min

## Summary

3 findings · 1 auto-fixed (§5, PR #950) · 2 reported · 3 informational  
Codex sweep: 0 fix / 0 reply / 0 skip / 0 deferred (window 2026-07-16 → 2026-07-18, all open-PR Codex comments are outside 48 h window)

## §5 auto-fixed (PRs)

- **#950** `fix(a11y): add missing width/height to 3 img tags (CLS prevention)`
  - `apps/restaurant-web/src/app/r/[code]/page.tsx:276` — hero image on referral landing, `width=920 height=517`
  - `apps/restaurant-admin/src/components/feedback-modal.tsx:280` — screenshot preview, `width=640 height=360`
  - `apps/restaurant-admin/src/app/dashboard/settings/presentation/presentation-client.tsx:374` — team member avatar, `width=48 height=48`

## §6 Codex sweep

Window 2026-07-16T00:00:00Z → 2026-07-18T00:27:46Z · PRs scanned: #941, #940, #939, #923, #921, #885

| PR | Comment date | Classification | Action | Reason |
|---|---|---|---|---|
| #941 | 2026-06-20 | — | STALE | Outside 48 h window |
| #940 | 2026-06-17 (×2) | — | STALE | Outside 48 h window |
| #921 | 2026-06-11 | — | STALE | Outside 48 h window (already replied in 2026-06-13 run) |

No new Codex comments were posted in the last 48 hours.

## Report-only findings (prioritized)

### [P2] Migration sequence gap: 20260628_003 missing

- **Where**: `supabase/migrations/` — sequence for 2026-06-28 is `_001`, `_002`, `_004`, `_005` (no `_003`)
- **Symptom**: Gap in numeric sequence. Any tooling that validates monotonic migration numbering will flag this.
- **Suggested fix**: Investigate whether `_003` was intentionally skipped or lost. If lost, a new migration (dated after current latest) is needed. If intentional, no action required.
- **Why not auto-fixed**: Never write to `supabase/migrations/`.

### [P2] Migration duplicate prefixes: 34 total groups (was ~21 in 2026-06-20 report)

- **Where**: `supabase/migrations/` — 34 calendar days have two or more `.sql` files with the same `YYYYMMDD` prefix
- **Key known duplicate** (carryover): `20260630_030_courier_dropoff_notes.sql` vs `20260630_030_courier_offer_push_trigger.sql`
- **Symptom**: On a clean migration replay, ordering within a day is determined alphabetically. Duplicate `_NNN` counters within a day mean two migrations have the same position and their ordering is non-deterministic (alphabetical fallback).
- **Suggested fix**: Rename one file in each conflicting pair to use the next available sequence number.
- **Why not auto-fixed**: Never write to `supabase/migrations/`.

## §7 Improvement suggestions

### Modifier-groups CRUD UI — RESOLVED (6+ run carryover)

`apps/restaurant-admin/src/app/dashboard/menu/modifiers-panel.tsx` is now 563 lines with full CRUD (`createModifierAction`, `updateModifierAction`, `deleteModifierAction`, group equivalents). Migration `20260505_001_modifier_groups.sql` also in place. **This carryover item is now closed.** 🎉

### i18n drift — RESOLVED

RO=968, EN=968 — keys are now equal. Last run showed drift of 10. No action needed.

### Untested API routes (restaurant-web)

Routes lacking a regression spec file (compared to test-covered peers):
- `/api/checkout/cancel-resume/route.ts`
- `/api/checkout/netopia/intent/route.ts`
- `/api/checkout/otp/request/route.ts` and `otp/verify/route.ts`
- `/api/checkout/pre-order/route.ts`
- `/api/checkout/promo/validate/route.ts`
- `/api/checkout/quote/route.ts`
- `/api/public/v1/orders/[id]/route.ts`
- `/api/courier-track/[ctoken]/route.ts` and `messages/route.ts`

Most are in the purchase-funnel critical path. Worth scheduling a test sprint before increasing traffic.

### PR #940 (security advisor hardening) — still open

Two Codex P1 comments (2026-06-17, outside sweep window) flagging that `20260617_001_security_advisor_safe_hardening.sql` alters functions that either don't exist in the repo (`copilot_tenant_config_set_updated_at`) or are created by later-dated migrations (`20260630_003`+). These were already surfaced in the 2026-06-20 run. This PR should be addressed before merge to avoid clean-replay failures.

### POS adapters in integration-core

Five real adapters (`freya`, `iiko`, `posnet`, `smartcash`, `custom`) exist under `packages/integration-core/src/adapters/`. Worth verifying at least one is exercised against a seed/dev database to confirm the scaffold functions end-to-end. No `integration_providers` seed data found in scope.

### Deferred UX items from UI/UX audit still outstanding

From `docs/UI_UX_AUDIT_PROGRESS.md`:
- **§4 Checkout P0**: sticky-bottom CTA on mobile + cart-summary collapsible accordion (noted as deferred for 2+ runs)
- **§9 Menu P0**: drag-to-reorder (noted as deferred, needs schema or client-only solution)
- **§9 Menu P1**: inline-editable price

These are high-visibility storefront items; the pilot being live makes them higher-priority than deferred UX polish typically is.

### Open housekeeping PRs

PRs #923 (nightly QA 2026-06-13) and #885 (nightly QA 2026-06-06) are still open. Both are safe to merge or close at discretion — no blocking content.

## Run metadata

| Step | Result |
|---|---|
| `git remote -v` isolation check | ✅ RSHIR-Platform confirmed |
| `pnpm install --frozen-lockfile` | ✅ Done in 10.5s |
| `pnpm -r typecheck` | ✅ All packages clean |
| `pnpm --filter @hir/restaurant-web build` | ✅ Green |
| `pnpm --filter @hir/restaurant-admin build` | ✅ Green |
| `pnpm --filter @hir/restaurant-web lint` | ✅ No warnings or errors |
| `pnpm --filter @hir/restaurant-admin lint` | ✅ No warnings or errors |
| Concurrency check (last 30 min activity) | ✅ No concurrent human/agent activity |
| §4I img CLS scan (restaurant-web + admin) | 3 issues found → PR #950 |
| §4H defensive-SELECT (payment_method) | ✅ cod-pending-panel.tsx and orders/actions.ts both have proper error fallbacks |
| §4E i18n key parity | ✅ RO=968 EN=968 (equal, drift resolved) |
| §4J migration sequence | 34 duplicate prefix groups; gap at 20260628_003 |
| §6 Codex sweep (48 h window) | 0 comments in window |
| §5 safety net (post-fix typecheck + builds) | ✅ Passed |
