# Nightly QA report — 2026-08-08

Starting commit · `e7c9964` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~30 min

## Summary

**4 findings · 1 auto-fixed (§5, 1 PR) · 2 reported · 1 informational**
Codex sweep: 0 fix / 0 reply / 0 skip / 0 deferred

## Build health

| Check | Result |
| --- | --- |
| TypeScript (all packages) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint both apps | ✅ CLEAN |
| Purchase funnel routes (cart→checkout→/track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ All 32 hrefs resolve to `page.tsx` |
| Defensive SELECT (payment_method / COD / modifier_groups) | ✅ All wrapped in try/catch |
| i18n drift RO vs EN | ✅ RO=942 / EN=942 / Δ=0 |
| Migration sequence integrity | ⚠️ 36+ duplicate prefix groups (carry-over — see P2 below) |

---

## §5 Auto-fixed (PRs)

- **#1065** `fix(a11y): add missing width/height to 3 img tags (CLS prevention)` — `hero-showcase.tsx` (cover 320×80), `tenant-header.tsx` (cover-logo 140×40), `courier-track-view.tsx` (avatar 36×36)

---

## §6 Codex sweep

Window `2026-08-06T00:00:00Z` → `2026-08-08T00:25:58Z` · PRs scanned: 0 (no open PRs updated in the 48 h window — most recent open PR #1026 was last updated 2026-08-01)

| PR | file:line | classification | action | link |
| --- | --- | --- | --- | --- |
| — | — | — | — | — |

---

## Report-only findings (prioritized)

### [P2] Migration duplicate prefix groups (carry-over from prior reports)

**Where:** `supabase/migrations/*.sql`
**Symptom:** 36+ groups of 2–3 files share the same `YYYYMMDD_NNN` prefix. Non-deterministic apply order on a fresh Supabase restore. New group added this week: `20260730_002` (`delivery_zone_tier_order_tiebreak.sql` + `loyalty_earn_idempotency.sql`) — both from the checkout-pricing fix (#1059).
**Why not auto-fixed:** Migration renames require verifying and re-applying affected statements on all Supabase environments; human must sign off before rename.
**Suggested fix:** Rename later files in each group to the next available sequence (`_002`, `_003`, etc.) after confirming the apply order on staging.

### [P2] Admin modifier-groups CRUD UI missing (carry-over)

**Where:** `apps/restaurant-admin/src/app/dashboard/menu/modifier-groups/` — does not exist.
**Symptom:** Migration `20260505_001_modifier_groups.sql` has shipped and `restaurant_menu_modifier_groups` is queried in `menu/actions.ts` and `menu/page.tsx`, but there is no dedicated CRUD page for operators to add/edit/delete modifier groups. The fix from #1051 made them *visible* to customers; this is the missing admin side to *manage* them.
**Suggested fix:** ~1-day effort following the existing `promos/` pattern (list + add/edit dialog, server actions, revalidation).
**Why not auto-fixed:** Exceeds 3-file/50-line cap; requires new page, actions, form.

---

## §7 Improvement suggestions

Non-bug. Tied to UX audit / conversion research / roadmap.

### 1. Untested public API routes (test coverage gap)
40 of ~50 `apps/restaurant-web/src/app/api/**/route.ts` files have no test file. The highest-risk untested routes are in the authentication and checkout flows:
- `/api/account/phone-login/verify/` — OTP verification
- `/api/checkout/otp/request|verify/` — checkout phone confirmation
- `/api/checkout/cancel-resume/` — order cancellation
- `/api/checkout/promo/validate/` — promo code validation
- `/api/courier-track/[ctoken]/route.ts` — courier position polling

The checkout and track routes already have good coverage; the account/auth flow is the largest blind spot.

### 2. Deferred UX audit items now actionable
From `docs/UI_UX_AUDIT_PROGRESS.md`:
- **§4 Checkout P0 sticky-bottom CTA on mobile** — marked deferred "post-pilot"; checkout flow is now stable post-OTP/PSP. Estimated 1-day effort.
- **§4 Checkout P0 cart-summary collapsible accordion** — same gate, now passable.
- **§9 Menu drag-to-reorder (P0)** — deferred; drag-to-reorder on menu items is a frequently-requested UX; could be done with `@dnd-kit/core` which is already a common SaaS pattern.

### 3. POS adapters: SmartCash is the only registered adapter
`packages/integration-core/src/adapters/` has: `custom.ts`, `freya.ts`, `iiko.ts`, `mock.ts`, `posnet.ts`, `smartcash.ts`, `registry.ts`. SmartCash is the only one explicitly mentioned in integration provider checks. Freya/iiko/posnet adapters exist as stubs. If `integration_providers` returns zero active rows in dev seed, the adapter scaffolding is untested in real conditions.

### 4. Deferred: `docs/UI_UX_AUDIT_PROGRESS.md` — §8 Orders queue sound + tab-title flash (P1)
Kitchen notification sound + browser-tab title flash on new order were deferred. Now that the aggregator and POS order silence bug is fixed (#1063), these notifications will fire more frequently and the sound/flash will have higher impact.

---

## Run metadata

```
pnpm install --frozen-lockfile          ✅ Done in 14.9s
pnpm -r typecheck                       ✅ All 16 packages clean
pnpm --filter @hir/restaurant-web build ✅ Clean
pnpm --filter @hir/restaurant-admin build ✅ Clean
pnpm -r lint                            ✅ No ESLint errors/warnings in apps
§4.D Link walk (32 admin hrefs)         ✅ All resolve to page.tsx
§4.E i18n key check                     ✅ RO=942 EN=942 Δ=0
§4.H Defensive SELECT audit             ✅ All modifier_groups queries wrapped in try/catch
§4.I CLS img audit                      3 found → auto-fixed (PR #1065)
§6 Codex sweep                          0 open PRs in 48h window
```
