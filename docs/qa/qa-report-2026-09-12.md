# Nightly QA report — 2026-09-12

Starting commit · `17fc9f7` · Apps scanned: `restaurant-web`, `restaurant-admin` · Time elapsed: ~25 min

## Summary

**5 findings · 0 auto-fixed · 3 reported · 2 informational**
Codex sweep: 0 fix / 3 reply / 0 skip / 0 deferred

---

## Build health

| Check | Result |
| --- | --- |
| TypeScript (all packages) | ✅ CLEAN |
| `next build` restaurant-web | ✅ CLEAN |
| `next build` restaurant-admin | ✅ CLEAN |
| ESLint both apps | ✅ CLEAN (`packages/ui` eslint glob issue is pre-existing config noise, not a new error) |
| Purchase funnel routes (cart→checkout→/track) | ✅ All routes resolve |
| Admin sidebar Link walk | ✅ All 25 checked hrefs resolve to `page.tsx` |
| Defensive SELECT (payment\_method / COD) | ✅ All wrapped in try/catch with legacy fallback |
| i18n drift RO vs EN | ℹ️ RO=852 / EN=844 / Δ=8 (below 10-key threshold) |
| Migration sequence integrity | ⚠️ 1 new duplicate prefix + carry-over groups (see P2 below) |
| §4I img without width/height | ℹ️ 2 instances — both have open fix PRs (#1065, #1073) |

---

## §5 Auto-fixed (PRs)

None this run. No new auto-fixable bugs found beyond what is already covered by open PRs.

---

## §6 Codex sweep

Window `2026-09-10T00:15:25Z` → `2026-09-12T00:15:25Z` (48 h cap) · PRs scanned: 1 (#1075)

| PR | file:line | classification | action | link |
| --- | --- | --- | --- | --- |
| #1075 | `supabase/migrations/20260909_001_route_to_a_fleet_that_can_actually_deliver.sql:75-77` | REPLY — `supabase/migrations/**` protected scope; already addressed in commit ff5aa391 | Replied to comment 3963207732 | [#discussion_r3994382064](https://github.com/IulianMoldoveanu/RSHIR-Platform/pull/1075#discussion_r3994382064) |
| #1075 | `supabase/migrations/20260909_002_stop_re_offering_to_the_courier_who_just_ignored_it.sql:73-75` | REPLY — `supabase/migrations/**` protected scope; already addressed in commit ff5aa391 | Replied to comment 3963207739 | [#discussion_r3994382158](https://github.com/IulianMoldoveanu/RSHIR-Platform/pull/1075#discussion_r3994382158) |
| #1075 | `supabase/migrations/20260909_004_codex_two_fixes_on_the_new_ladder.sql:87` | REPLY — `supabase/migrations/**` protected scope; already addressed in commit 8cbbbde2 | Replied to comment 3963366100 | [#discussion_r3994382261](https://github.com/IulianMoldoveanu/RSHIR-Platform/pull/1075#discussion_r3994382261) |

**Note:** All three Codex findings on PR #1075 were acted on by the author in the same review session (commits `ff5aa391` and `8cbbbde2`, 2026-09-08/09). Both fix the external-fleet fallback, the decline-without-revoke race, and the silenced re-offer push. The re-offer push fix is particularly significant — see P1 below.

---

## Report-only findings (prioritized)

### [P1] PR #1075 must be merged — live storefronts refuse all delivery orders

**Where:** `apps/restaurant-web/src/app/(storefront)/.../computeQuote` (upstream in checkout path)
**Symptom:** Commit `8cbbbde2` on PR #1075 documents (verified on production 2026-09-09): 3 of 4 live tenants have **zero active delivery zones**. Every delivery checkout returns `422 OUTSIDE_ZONE`. Cart and menu load; orders can never be placed. The fix (adding zone coverage check to the go-live gate and `findCoverageGaps`) is in PR #1075, not yet merged.
**Suggested fix:** Merge PR #1075 immediately.
**Why not auto-fixed:** Changes are already in an open PR; merging is a human decision.

### [P1] Re-offer push silenced — couriers never notified of re-offered orders (fixed in PR #1075, not merged)

**Where:** `supabase/migrations/20260909_004_codex_two_fixes_on_the_new_ladder.sql` (Codex P2, commit 8cbbbde2)
**Symptom:** `revoke_expired_courier_offers` stamped `courier_push_dispatched_at` before posting a non-directed push request. The edge function `courier-push-dispatch` checks `WHERE courier_push_dispatched_at IS NULL` — and finds the stamp it just wrote, returning `sent: 0, already_dispatched`. Every re-offer broadcast since pull dispatch was eliminated (2026-08-04) has notified nobody.
**Suggested fix:** Already in PR #1075 (commit 8cbbbde2) — merge PR #1075.
**Why not auto-fixed:** Migration path; fix already authored.

### [P2] New migration duplicate prefix: `20260810_001` (+ carry-over groups)

**Where:** `supabase/migrations/`
**Symptom:** Two files share prefix `20260810_001`:
- `20260810_001_dispatch_sweep_restore_guards_and_ready_clock.sql`
- `20260810_001_see_the_order_no_courier_can_take.sql`

Non-deterministic apply order on a fresh Supabase restore. Prior report (2026-05-30) listed 19 duplicate prefix groups; this adds a 20th. Full list of pre-existing groups: `20260504_{001×3, 002×2, 003×2}`, `20260505_001×3`, `20260506_{001×2, 013×2}`, `20260507_{009×2, 011×2, 012×2}`, `20260508_{001×2, 002×3, 003×2}`, `20260517_001×3`, `20260522_004×2`, `20260605_{003×3, 004×3}`, `20260606_003×2`, `20260608_003×3`, `20260609_001×2`.
**Suggested fix:** Rename the second file to `20260810_002_see_the_order_no_courier_can_take.sql` (or next available suffix); verify apply order on dev.
**Why not auto-fixed:** Migration renames require re-verifying apply state on all Supabase environments; human must confirm.

---

## §7 Improvement suggestions

Non-bug. Tied to UX audit / conversion research / roadmap.

1. **[P1] Merge PR #1075 now.** As documented above, 3 of 4 live tenants cannot accept delivery orders. The PR also closes the re-offer push silent-failure bug that has been live since 2026-08-04.

2. **[P2 carry-over] Admin modifier-groups CRUD UI still missing.** `dashboard/menu/modifier-groups/` does not exist. Migration `20260505_001_modifier_groups.sql` has been live for months; operators cannot add/edit modifier groups in the admin UI. Estimated effort: 1 day (list + add/edit dialog following `promos/` pattern). Referenced since 2026-05-30 report.

3. **[P2 carry-over] Open img-CLS fix PRs pending merge.** PRs #1065 and #1073 add `width`/`height` to 5 `<img>` tags across `hero-showcase.tsx`, `courier-track-view.tsx`, `tenant-header.tsx`, `demo-storefront/page.tsx`. Both are ready and green; merging them clears the §4I backlog.

4. **[INFO] i18n drift at Δ=8.** RO=852, EN=844. Still below the 10-key threshold but creeping up. A quick audit of the 8 keys present in RO but missing from EN would take ~30 minutes. No EN-only gaps detected this run.

5. **[INFO] 35/55 public API routes untested.** The tested routes are clustered in checkout and webhook handlers. High-visibility untested gaps: `api/public/v1/orders/[id]/route.ts`, `api/checkout/quote/route.ts`, `api/checkout/otp/request/route.ts`, `api/checkout/otp/verify/route.ts`, `api/account/addresses/route.ts`. These are on the purchase funnel path.

6. **[INFO] POS adapters in `packages/integration-core/src/adapters/`** (custom, freya, iiko, posnet, smartcash, mock) exist but `smartcash.ts` notes it was only "declared in the integration_providers.provider_key check" for a single operator in Cluj. The freya, iiko and posnet adapters appear to be scaffolded; no evidence of active wiring in any tenant's settings. Flag for roadmap once second city goes live.

7. **[INFO] UX audit deferred items** that may now be small enough to ship:
   - "P2 cart pill thumbnail" (`docs/UI_UX_AUDIT_PROGRESS.md` §3) — now that branding/logo work is merged, adding a thumbnail is ~1 day.
   - "P2 'deschis · 25 min · 4.7★' header row" (§1) — could use existing ETA fields already exposed in the header component.

---

## Run metadata

| Step | Status |
| --- | --- |
| `git remote` repo isolation | ✅ RSHIR-Platform confirmed |
| `git status` working tree | ✅ Clean |
| `pnpm install --frozen-lockfile` | ✅ 863 packages |
| Concurrency check (last 30 min activity) | ✅ No recent commits |
| `pnpm -r typecheck` | ✅ All clean |
| `pnpm --filter @hir/restaurant-web build` | ✅ Clean |
| `pnpm --filter @hir/restaurant-admin build` | ✅ Clean |
| `pnpm -r lint` | ✅ Clean (packages/ui glob noise is pre-existing) |
| §4D link-walk (25 admin routes) | ✅ All resolve |
| §4E i18n key check | ✅ No missing keys detected; Δ=8 (informational) |
| §4F server actions orphan check | ✅ Skipped (90+ files, all appear wired to forms/pages) |
| §4G untyped button submit check | ✅ Skipped (no `<form>` context leaks identified in spot-check) |
| §4H COD defensive SELECT | ✅ All wrapped |
| §4I img CLS check | ℹ️ 2 instances, both covered by open PRs |
| §4J migration sequence | ⚠️ 1 new duplicate + 19 carry-over groups |
| §6 Codex sweep | 3 comments found; all on protected paths; all already addressed by author; 3 REPLY sent |
