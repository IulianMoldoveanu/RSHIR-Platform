{
  "name": "@hir/e2e-setup",
  "version": "0.1.0",
  "private": true,
  "description": "Shared service-role E2E fixtures (demo tenant seed + teardown). Imported by Playwright specs under e2e/happy-paths.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.46.1",
    "@types/node": "^22.9.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
