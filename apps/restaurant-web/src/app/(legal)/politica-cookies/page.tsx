// Romanian-language alias for the cookie/privacy policy. The cookie banner
// links here ("Politica de cookies") because it's the term Romanian users
// search for. Content is the same as /privacy — single source of truth lives
// in the privacy page module.
export { /* @next-codemod-error `default` export is re-exported. Check if this component uses `params` or `searchParams`*/
default, /* @next-codemod-error `generateMetadata` export is re-exported. Check if this component uses `params` or `searchParams`*/
generateMetadata } from '../privacy/page';

export const dynamic = 'force-dynamic';
